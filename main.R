#Load dependencies
source("config.R")
source("classes.R")

#Run
main <- function(){
  
  #Construct directories
  dir.create(LOG_DIR, showWarnings = FALSE)
  dir.create(SAVE_DIR, showWarnings = FALSE)
  
  #Desktop
  
  #Apps
  
  #Mobile
}